package core

import (
	"testing"
)

func TestXChain_Success(t *testing.T) {

	//create lua state:
	l := createLuaState()
	defer l.Close()

	//create the modules:
	core := Register(l)

	//execute the chunk:
	executeChunkForTests(l, "lua/xchain_test.lua")
}
